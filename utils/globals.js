// * This is a Global Variables file

const Global = {
  albumsExtracted: 0, // Num of albums stored in JSON file
  albumsVisited: 0,  // Num of album links visited
  iteration: 0  // Num of times we visited the /releases page
}

export default Global;